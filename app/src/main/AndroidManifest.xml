<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".gggggggg1"></activity>
        <activity android:name=".HondaActivity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Honda" />
        <activity android:name=".CalculateMainActivity" />
        <activity android:name=".ToyotaActivity" />
        <activity android:name=".NissanActivity" />
        <activity android:name=".MasdaActivity" />
        <activity android:name=".CarBrandActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>